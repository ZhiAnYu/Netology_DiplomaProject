spring:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  jackson:
    parameter-names:
      mode: all

logging:
  level:
    root: WARN
    ru.netology.cloudService: DEBUG

file-storage:
  path: ./test-files