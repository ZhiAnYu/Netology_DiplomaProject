spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  jackson:
    parameter-names:
      mode: all

logging:
  level:
    root: WARN
    ru.netology.cloudService: INFO

file-storage:
  path: ./test-files