# Настройки для тестов (переопределяют основные)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: WARN
    ru.netology.cloudservice: INFO

file-storage:
  path: ./test-files                     